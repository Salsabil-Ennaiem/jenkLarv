# Prompt: Generate Laravel CRUD Controller
Use Laravel 12 best practices. Include form requests, middleware for auth, and Spatie permissions if detected.
Model: {{model_name}}
Relations: {{relations}}
Always add image upload with Intervention if field exists.